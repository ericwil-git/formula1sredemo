{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "CacheDirectory": "D:\\f1-files",
  "LogDirectory": "D:\\f1-files\\logs",
  "FileGenerator": {
    "_comment": "ApiKey + SqlConnectionString are injected at runtime via env vars (FileGenerator__ApiKey, FileGenerator__SqlConnectionString) populated from Key Vault by the VM bootstrap script. Leave blank here so we don't ship secrets in the repo.",
    "ApiKey": "",
    "SqlConnectionString": ""
  }
}
